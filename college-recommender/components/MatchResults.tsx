import type { RecommendationResponse } from "@/lib/contract";

/**
 * Stub — Task 12 builds the real results list, compare tray and compare
 * table. This placeholder exists only so ProfileForm (Task 10) has something
 * to render and import after a successful submit; it renders nothing.
 */
export function MatchResults(props: { response: RecommendationResponse }): null {
  void props;
  return null;
}
