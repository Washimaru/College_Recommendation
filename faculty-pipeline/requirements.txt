# Mirrors [project.dependencies] in pyproject.toml. See
# docs/FACULTY_PIPELINE.md §10; `pip install -e ".[dev]"` is the supported
# install path (this file is kept for parity with data-pipeline/).
httpx>=0.27
selectolax>=0.3
tenacity>=8.2
pydantic>=2.6
click>=8.1
pyyaml>=6.0
anthropic>=0.34
python-dotenv>=1.0
tldextract>=5.1
# optional: headless fallback for JS-rendered directories (--dynamic)
# playwright>=1.44
