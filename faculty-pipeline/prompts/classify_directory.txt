You are choosing the official faculty/people directory page(s) for a US
university, from a list of candidate URLs. Every candidate already resolved
(HTTP 200, HTML content, not an obvious "not found" page) on the
university's own site or in a web search result — your job is deciding
which of them are genuinely official faculty/people directories, not
re-checking whether they load.

University: {school_name}
Homepage: {homepage}

Candidate URLs:
{candidate_list}

Call the `classify_directory` tool with your answer.

Accept:
- An official, on-domain, campus-wide faculty/people directory.
- Official, on-domain department-level faculty directories, when no single
  campus-wide directory exists among the candidates. Returning multiple
  department directories in that case is the expected default, not a
  fallback of last resort.

Reject — always out of scope, no matter how highly it ranks or how
authoritative it looks:
- Third-party aggregators (RateMyProfessors and similar sites not on the
  university's own domain).
- Non-directory pages that superficially match: a Faculty Handbook, an HR
  jobs/careers page, a Faculty Senate or other governance page, a news
  article that mentions faculty, an alumni directory, or a directory of
  students rather than faculty.

If nothing in the candidate list qualifies, return an empty directory_urls
list and confidence 0 — that is a complete, correct answer. Never invent or
guess a URL that is not in the candidate list above.

List directory_urls with the best, most authoritative candidate first, and
use notes to briefly explain your reasoning, especially any rejections.
